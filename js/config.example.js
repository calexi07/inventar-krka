// Copy this file to `config.js` and fill in your project's values.
// `config.js` is gitignored — never commit real credentials.
//
// The anon/public key is safe to ship to the browser: it has no access to
// anything Row Level Security doesn't explicitly allow. Never put the
// service-role key in any file under js/.

export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
